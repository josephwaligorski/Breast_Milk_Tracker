# Runtime configuration for Breast Milk Tracker
# These values are used by docker-compose and the backend server

# App
NODE_ENV=production
PORT=5000

# Printing (CUPS)
# Set PRINTER to your CUPS queue name (lpstat -p)
PRINTER=PL420
# Default custom media size for 2.625" x 1" labels (in 1/72" points)
LABEL_MEDIA=Custom.189x72

# Printing mode: "tspl" for Polono PL420 raw commands, empty for PDF via CUPS
PRINT_MODE=tspl
# Optional orientation for PDF path: landscape or empty
ORIENTATION=
# For PDF path: 1 enables fit-to-page, 0 uses 100% scaling
PRINT_FIT=1

# Updates
# Enable one-click self-update from the UI (runs /app/update.sh if present, or docker compose up -d --build if permitted)
# WARNING: leave 0 unless you understand the implications and have secured the device
ENABLE_SELF_UPDATE=0

# Build metadata (injected at build time; not required to set here)
# BUILD_COMMIT=
# BUILD_TIME=
# BUILD_VERSION=
